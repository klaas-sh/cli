# klaas CLI Environment Configuration
#
# Copy this file to .env for local development:
#   cp .env.example .env
#
# These values are read at COMPILE TIME by build.rs and baked into the binary.
# After changing this file, you must rebuild: cargo build
#
# For release builds, these values are ignored - production URLs are hardcoded.

# API Base URL (default: http://localhost:8787)
KLAAS_API_URL=http://localhost:8787

# WebSocket URL (optional, derived from API URL if not set)
# KLAAS_WS_URL=ws://localhost:8787/ws
