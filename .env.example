# Nexo CLI Environment Configuration
#
# Copy this file to .env for local development:
#   cp .env.example .env
#
# Environment variables can also be set directly in your shell or CI/CD.

# API Base URL
# For local development with the API running on localhost:
NEXO_API_URL=http://localhost:8787

# For production (set in CI/CD, not in .env):
# NEXO_API_URL=https://api.nexo.dev

# WebSocket URL (optional, derived from API URL if not set)
# NEXO_WS_URL=ws://localhost:8787/ws
